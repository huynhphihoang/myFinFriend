# Add this code snippet to "test_EXAMPLE.py" file for correct import path configuration
# Terminal root should be in myFinFriend NOT subfolders

import sys
from pathlib import Path

# Add source directory to path
sys.path.insert(0, str(Path(__file__).parent.parent / "source"))